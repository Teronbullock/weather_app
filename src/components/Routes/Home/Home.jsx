import React from 'react';
import { Form } from '../../Form';
import AppBody from '../../AppBody';

export default props => {
  return (
    <React.Fragment>
      <Form />
      <AppBody />
    </React.Fragment>
  );
};
